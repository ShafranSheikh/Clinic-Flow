import SwiftUI

struct RecordsView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Spacer()
            }
            .navigationTitle("Records")
        }
    }
}

#Preview {
    RecordsView()
}
