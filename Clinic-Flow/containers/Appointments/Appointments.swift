import SwiftUI

struct AppointmentsView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Spacer()
            }
            .navigationTitle("Appointments")
        }
    }
}

#Preview {
    AppointmentsView()
}
